{
  "name": "jarvis-prime",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps",
    "packages/*"
  ],
  "scripts": {
    "dev:site": "npm run dev --workspace=apps",
    "dev:engine": "npm run server --workspace=packages/engine",
    "build:site": "npm run build --workspace=apps",
    "test:engine": "npm run test --workspace=packages/engine",
    "dev": "concurrently \"npm run dev:site\" \"npm run dev:engine\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}

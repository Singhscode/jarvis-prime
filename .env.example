# This repository has no root runtime environment file.
# Configure each application separately:
#   cp apps/api/.env.example apps/api/.env
#   cp apps/web/.env.example apps/web/.env.local
# Follow documentation/LOCAL_DEVELOPMENT.md to generate local-only values.

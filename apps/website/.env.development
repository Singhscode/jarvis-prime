# =============================================================================
# JARVIS PRIME SITE — DEVELOPMENT environment
# Safe defaults only — no real secrets.
# Real values go in .env.local (gitignored).
# =============================================================================

NODE_ENV=development

NEXT_PUBLIC_ENGINE_URL=http://localhost:3001

# Supabase — fill in .env.local
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Must match engine/.env AUTOMATION_SERVER_SECRET
AUTOMATION_SECRET=dev-secret-change-me

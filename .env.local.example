# =============================================================================
# JARVIS PRIME — Root-level environment (monorepo shared vars)
#
# Copy to .env.local. This file is safe to commit — no real secrets here.
# =============================================================================

# Used by concurrently / root npm scripts
NODE_ENV=development
